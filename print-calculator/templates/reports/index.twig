<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Relatórios de Orçamentos</title>
    <link rel="stylesheet" href="/assets/css/app.css">
</head>
<body>
    <div class="container mx-auto p-4">
        <h1 class="text-2xl font-bold mb-4">Relatórios de Orçamentos</h1>
        
        {% if reports is not empty %}
            <table class="min-w-full bg-white border border-gray-300">
                <thead>
                    <tr>
                        <th class="py-2 px-4 border-b">ID</th>
                        <th class="py-2 px-4 border-b">Data</th>
                        <th class="py-2 px-4 border-b">Cliente</th>
                        <th class="py-2 px-4 border-b">Total</th>
                        <th class="py-2 px-4 border-b">Ações</th>
                    </tr>
                </thead>
                <tbody>
                    {% for report in reports %}
                        <tr>
                            <td class="py-2 px-4 border-b">{{ report.id }}</td>
                            <td class="py-2 px-4 border-b">{{ report.date|date("d/m/Y") }}</td>
                            <td class="py-2 px-4 border-b">{{ report.client }}</td>
                            <td class="py-2 px-4 border-b">R$ {{ report.total|number_format(2, ',', '.') }}</td>
                            <td class="py-2 px-4 border-b">
                                <a href="/reports/view/{{ report.id }}" class="text-blue-500 hover:underline">Ver</a>
                                <a href="/reports/download/pdf/{{ report.id }}" class="text-green-500 hover:underline">PDF</a>
                                <a href="/reports/download/csv/{{ report.id }}" class="text-yellow-500 hover:underline">CSV</a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% else %}
            <p class="text-gray-500">Nenhum relatório disponível.</p>
        {% endif %}
    </div>
</body>
</html>