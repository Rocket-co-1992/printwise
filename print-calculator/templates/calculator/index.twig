<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Calculadora de Custos - Gráfica Digital</title>
    <link rel="stylesheet" href="/assets/css/app.css">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-4">
        <h1 class="text-2xl font-bold mb-4">Calculadora de Custos</h1>
        
        <form id="calculator-form" class="bg-white p-6 rounded shadow-md">
            <div class="mb-4">
                <label for="material" class="block text-sm font-medium text-gray-700">Material</label>
                <select id="material" name="material" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm">
                    <!-- Options will be populated dynamically -->
                </select>
            </div>
            <div class="mb-4">
                <label for="format" class="block text-sm font-medium text-gray-700">Formato</label>
                <select id="format" name="format" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm">
                    <!-- Options will be populated dynamically -->
                </select>
            </div>
            <div class="mb-4">
                <label for="finishing" class="block text-sm font-medium text-gray-700">Acabamento</label>
                <select id="finishing" name="finishing" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm">
                    <!-- Options will be populated dynamically -->
                </select>
            </div>
            <div class="mb-4">
                <label for="quantity" class="block text-sm font-medium text-gray-700">Quantidade</label>
                <input type="number" id="quantity" name="quantity" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm" required>
            </div>
            <button type="submit" class="w-full bg-blue-500 text-white py-2 rounded-md">Calcular Preço</button>
        </form>

        <div id="result" class="mt-6 hidden">
            <h2 class="text-xl font-bold">Resultado</h2>
            <p id="price" class="text-lg text-green-600"></p>
        </div>
    </div>

    <script src="/assets/js/calculator.js"></script>
</body>
</html>