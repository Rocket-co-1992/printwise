<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Acabamentos</title>
    <link rel="stylesheet" href="/assets/css/app.css">
</head>
<body>
    <div class="container mx-auto mt-5">
        <h1 class="text-2xl font-bold">Acabamentos</h1>
        <a href="/finishing/create" class="mt-3 inline-block bg-blue-500 text-white px-4 py-2 rounded">Adicionar Novo Acabamento</a>
        
        <table class="min-w-full mt-5 border border-gray-300">
            <thead>
                <tr class="bg-gray-200">
                    <th class="border px-4 py-2">ID</th>
                    <th class="border px-4 py-2">Nome</th>
                    <th class="border px-4 py-2">Ações</th>
                </tr>
            </thead>
            <tbody>
                {% for finishing in finishings %}
                <tr>
                    <td class="border px-4 py-2">{{ finishing.id }}</td>
                    <td class="border px-4 py-2">{{ finishing.name }}</td>
                    <td class="border px-4 py-2">
                        <a href="/finishing/edit/{{ finishing.id }}" class="text-yellow-500">Editar</a>
                        <form action="/finishing/delete/{{ finishing.id }}" method="POST" class="inline">
                            <button type="submit" class="text-red-500">Excluir</button>
                        </form>
                    </td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="3" class="border px-4 py-2 text-center">Nenhum acabamento encontrado.</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>