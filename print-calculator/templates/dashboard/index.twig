<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Calculadora de Custos</title>
    <link rel="stylesheet" href="/assets/css/app.css">
</head>
<body>
    <div class="container mx-auto p-4">
        <header class="mb-6">
            <h1 class="text-2xl font-bold">Dashboard</h1>
        </header>
        
        <section class="mb-6">
            <h2 class="text-xl font-semibold">Estatísticas</h2>
            <div id="statistics" class="grid grid-cols-1 md:grid-cols-3 gap-4">
                <!-- Estatísticas dinâmicas serão carregadas aqui via JavaScript -->
            </div>
        </section>

        <section>
            <h2 class="text-xl font-semibold">Histórico de Orçamentos</h2>
            <table class="min-w-full bg-white border border-gray-300">
                <thead>
                    <tr>
                        <th class="py-2 px-4 border-b">Data</th>
                        <th class="py-2 px-4 border-b">Cliente</th>
                        <th class="py-2 px-4 border-b">Valor</th>
                        <th class="py-2 px-4 border-b">Status</th>
                    </tr>
                </thead>
                <tbody id="quote-history">
                    <!-- Histórico de orçamentos será carregado aqui via JavaScript -->
                </tbody>
            </table>
        </section>
    </div>

    <script src="/assets/js/dashboard.js"></script>
</body>
</html>